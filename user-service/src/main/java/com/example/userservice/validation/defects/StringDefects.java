package com.example.userservice.validation.defects;

import com.example.userservice.validation.DefectId;

public enum StringDefects implements DefectId {
    INCORRECT_LENGTH,
    CONTAINS_FORBIDDEN_CHARACTERS
}
